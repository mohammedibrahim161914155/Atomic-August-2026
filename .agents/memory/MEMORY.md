- [Startup fix](startup-fix.md) — app fails to start when node_modules is missing/corrupt; `npm install --prefer-offline` restores it
- [MCP server transport](mcp-server.md) — atomic-mcp supports both stdio (default, for npx) and HTTP (MCP_TRANSPORT=http) transports; entry point is src/mcp/index.ts
- [Model registry pattern](model-registry.md) — src/models/registry.ts is the single source of truth for all model IDs; ModelEntry uses inputCostPer1M/outputCostPer1M (not pricing.*); MODEL_REGISTRY is an array — use .find(m => m.id === id)
- [Blueprint type shape](blueprint-type.md) — Blueprint.sections has fixed keys (executive_summary/architecture/data_model/api_contracts/security_model/edge_cases/testing_strategy/deployment/launch_checklist/technical_debt); pillars is Record<string,PillarOutput>; app name at intent.product_name
- [Vercel AI SDK v5 generateText](vercel-ai-sdk-v5.md) — generateText(prompt, config, systemInstruction?, options?); result is GenerateResult = { text: string; tokens_used: number } — NOT .content
- [Zod v4 z.record](zod-v4-record.md) — z.record() requires TWO args: z.record(keySchema, valueSchema); z.record(z.any()) causes TS2554 error in Zod v4
- [Pipeline routing pattern](pipeline-routing.md) — PipelineType flows from Landing → App.tsx → GenerationContext.start() → sse.startGeneration(); blueprint routes to /api/v1/generate, others to /api/v1/generate-pipeline with { prompt, pipelineType, mode }
- [MCP generate endpoint](mcp-generate-endpoint.md) — atomic_generate_blueprint must call /api/v1/generate-start (REST, returns sessionId immediately); NOT /api/v1/generate which is SSE-streaming and cannot be consumed by MCP clients
- [agentMemory API](agent-memory-api.md) — agentMemory.readDecisions(sessionId, '*') returns MemoryEntry[]; MemoryEntry fields are key/value/agent/pillar/ts — NOT decision; agentMemory.flagConcern/readConcerns for concerns
- [GovernorIntent fields](governor-intent-fields.md) — GovernorIntent has core_problem (not problem_statement), product_name, domain, target_users, key_features, tech_constraints, scale_assumptions, compliance_requirements, integration_targets, success_definition
- [costBudget API](cost-budget-api.md) — estimatePipelineCost(opts: RunCostOptions) where opts has { fastModel, proModel, mode, pillarCount, agentsPerPillar, promptLengthChars }; CostEstimate returns estimatedTotalUsd (not estimatedCostUsd)
- [withRetry signature](withretry-signature.md) — withRetry(fn, signal?, label?, overrides?): second arg is AbortSignal NOT options; pass undefined for signal when not cancelling
- [Vercel AI SDK streamText textStream](vercel-aisdk-textstream.md) — result.textStream is AsyncIterable<string> & ReadableStream<string>, NOT AsyncIterableIterator; return it directly, never call [Symbol.asyncIterator]() on it
- [v4 engine components](v4-engine-components.md) — eventBus/skills/observability/artemis/curator/blueprintVersions in src/engine/; Chat page at /chat; Skills section in Settings; VersionHistory slide-over in Blueprint; all API routes under /api/v1/
- [SDK and Plugins layout](sdk-plugins-layout.md) — SDK at src/sdk/ (AtomicClient with resources); Plugins at src/plugins/ (registry singleton + built-in/export-markdown, export-linear, export-notion); initBuiltInPlugins() called at startup
- [v4 audit fixes](v4-audit-fixes.md) — tokenBudgetManager.onBudgetWarning was broken (dynamic import returning Promise); projects.switchProject had previousProjectId captured after update; custom skills now SQLite-backed; Artemis workspace now SQLite+cache; blueprints have project_id column (migration 009/010)
- [AtomicEventType usage](atomic-event-type.md) — use 'agent.tool_call_started' and 'agent.tool_call_completed' (not 'agent_tool_call'); GovernorIntent.target_users is string not string[]; curator workspaces are now SQLite-backed via curator_workspaces table
- [LTM and sub-agent pattern](ltm-subagent-pattern.md) — agentLongTermMemory: rememberFact(domain, key, value, agent, importance, tags?); formatLongTermContext(domain?, maxItems?) returns string; LTM GET/DELETE at /api/v1/agent/memory/long-term; sub-agents use Promise.allSettled alongside main call
